const questionTypes = [/*'text',*/ 'radiogroup', 'checkbox', 'boolean']

const QuestionType = {
   Text: 'text',
   RadioGroup: 'radiogroup',
   Checkbox: 'checkbox',
   Boolean: 'boolean'
}

export { questionTypes, QuestionType }
